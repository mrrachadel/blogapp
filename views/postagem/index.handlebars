<div class="card">
  <div class="card-body">
    <h1>{{postagem.titulo}}</h1>
    <hr>
    <small style="float: right">Publicado em: {{postagem.data}}</small><br>
    <hr>
    <p>{{postagem.conteudo}}</p>
  </div>
</div>
